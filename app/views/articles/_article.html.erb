<p id="notice"><%= notice %></p>

<h1><%= article.title %></h1>

  <%= article.body %>


<%= link_to 'Show | ', article if can? :edit, @article %>
<%= link_to 'Edit | ', edit_article_path(article) if can? :edit, @article %>
<%= link_to 'Back | ', articles_path if can? :edit, @article %>
<%= link_to 'Destroy | ', article, :confirm => 'Are you sure?', :method => :delete if can? :edit, @article %>
